# Cluster Pilot Configuration Example
# Copy this file to .env and customize as needed

# Disable proxy for local Kubernetes connections
# Set these to empty strings to prevent corporate proxy from intercepting local traffic
# HTTP_PROXY=
# HTTPS_PROXY=
# http_proxy=
# https_proxy=

# Optional: Add no-proxy list for local hosts
# NO_PROXY=localhost,127.0.0.1,kubernetes.docker.internal,host.docker.internal,::1
# no_proxy=localhost,127.0.0.1,kubernetes.docker.internal,host.docker.internal,::1

# Custom kubeconfig path (optional)
# KUBECONFIG=C:\path\to\your\kubeconfig
